/* Psyche landing motion — placeholder.
   The cinematic scroll sequence (GSAP/ScrollTrigger + Lenis) lands here in the
   signature-UX phase. Kept as a guarded no-op for now so index.html has no
   dangling 404 and so app.js owns rendering until the sequence is built. */
(() => {
  'use strict';
  // Intentionally empty. See docs/redesign-spec.md.
})();
